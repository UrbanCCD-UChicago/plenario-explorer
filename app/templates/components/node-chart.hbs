{{#if (eq downloadingState '')}}

{{/if}}

{{#if (eq viewType 'live')}}
  {{stream-spinner streams=streams}}
{{else}}
  {{stream-spinner streams=streams}}
{{/if}}

{{#each-in streams as |propName propObj|}}
  {{#if (eq viewType 'live')}}
    {{stream-chart property=propObj viewType=viewType}}
  {{else}}
    {{stream-chart property=propObj viewType=viewType}}
  {{/if}}
{{/each-in}}