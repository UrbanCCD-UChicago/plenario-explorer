{{#unless dataArrived}}
  {{#if didTimeOut}}
      <div class="alert alert-warning" role="alert">
          Sorry, we couldn't find data for this node in this time range.
      </div>
  {{else}}
    {{spin-spinner lines=13 length=20 width=10}}
  {{/if}}
{{/unless}}