<div class="row">
  <div class="col-md-5">
    {{filter-box startDate=startDate endDate=endDate aggOptions=aggOptions agg=agg}}
  </div>
  <div class="col-md-7 half-height">
    {{leaflet-map lat=lat lng=lng zoom=zoom tileURL=tileURL
    drawnShape=drawnShape shapeSubmitted=shapeSubmitted}}
  </div>
</div>

{{input type="checkbox" name="shapeSubmitted" checked=shapeSubmitted}}
<p>Agg unit is {{agg}}. And {{startDate}} {{endDate}}</p>

{{outlet}}
