import { timespan } from '../../../helpers/timespan';
import { module, test } from 'qunit';

module('Unit | Helper | timespan');

// Replace this with your real tests.
test('it works', (assert) => {
  const result = timespan([42]);
  assert.ok(result);
});
