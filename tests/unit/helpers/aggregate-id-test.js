import { aggregateId } from '../../../helpers/aggregate-id';
import { module, test } from 'qunit';

module('Unit | Helper | aggregate id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = aggregateId([42]);
  assert.ok(result);
});
