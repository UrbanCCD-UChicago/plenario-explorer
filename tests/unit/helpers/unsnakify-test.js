import {unsnakify} from "plenario-explorer/helpers/unsnakify";
import {module, test} from "qunit";

module('Unit | Helper | unsnakify');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = unsnakify([42]);
  assert.ok(result);
});
