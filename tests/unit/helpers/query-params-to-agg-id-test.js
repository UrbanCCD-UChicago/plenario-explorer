import { queryParamsToAggId } from '../../../helpers/query-params-to-agg-id';
import { module, test } from 'qunit';

module('Unit | Helper | query params to agg id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = queryParamsToAggId([42]);
  assert.ok(result);
});
