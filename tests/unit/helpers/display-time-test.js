import {displayTime} from '../../../helpers/display-time';
import {module, test} from 'qunit';

module('Unit | Helper | display time');

// Replace this with your real tests.
test('it works', function (assert) {
  let result = displayTime([42]);
  assert.ok(result);
});
