import { stringify } from '../../../helpers/stringify';
import { module, test } from 'qunit';

module('Unit | Helper | stringify');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stringify([42]);
  assert.ok(result);
});
