import { aggIdToQueryParams } from '../../../helpers/agg-id-to-query-params';
import { module, test } from 'qunit';

module('Unit | Helper | agg id to query params');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = aggIdToQueryParams([42]);
  assert.ok(result);
});
