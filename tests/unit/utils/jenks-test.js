import jenks from 'plenario-explorer/utils/jenks';
import { module, test } from 'qunit';

module('Unit | Utility | jenks');

// Replace this with your real tests.
test('it works', (assert) => {
  const result = jenks([1, 1, 1, 1], 4);
  assert.ok(result);
});
